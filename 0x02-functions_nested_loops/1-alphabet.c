#include "main.h"

/**
 * main - check the code
 *
 * Return: always 0.
 * 
 */

int main(void)
{
	int naruto;
	for (naruto = 97; naruto <= 122; naruto++)
		putchar(naruto);
	putchar(10);

	return (0);
}
