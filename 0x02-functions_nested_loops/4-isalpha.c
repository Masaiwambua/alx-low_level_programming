#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: always 0.
 * 
 */
int main(void)
{
	int i = 0;

	while (i <= 9)
	{{
		int ch;
		for (ch = 97; ch <= 122; ch++)
			putchar (ch);}
	i++;
	putchar (10);

	}
	 return (0);
}
